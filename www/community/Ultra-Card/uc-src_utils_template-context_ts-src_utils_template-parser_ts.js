"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunkultra_card"] = self["webpackChunkultra_card"] || []).push([["src_utils_template-context_ts-src_utils_template-parser_ts"],{

/***/ "./src/utils/template-context.ts":
/*!***************************************!*\
  !*** ./src/utils/template-context.ts ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   buildEntityContext: () => (/* binding */ buildEntityContext),\n/* harmony export */   buildMultiEntityContext: () => (/* binding */ buildMultiEntityContext),\n/* harmony export */   isJsonResult: () => (/* binding */ isJsonResult),\n/* harmony export */   parseJsonResult: () => (/* binding */ parseJsonResult)\n/* harmony export */ });\n/**\n * Builds entity context variables for unified templates\n * Provides access to entity data, attributes, and helper functions\n * Similar to Mushroom Card's template system\n */\n/**\n * Build complete entity context for template evaluation\n * @param entityId The entity ID to build context for\n * @param hass Home Assistant instance\n * @param config Optional config object (icon config, entity config, etc.)\n * @returns Context object with entity data and helper functions\n */\nfunction buildEntityContext(entityId, hass, config) {\n    var _a;\n    const entityState = (_a = hass === null || hass === void 0 ? void 0 : hass.states) === null || _a === void 0 ? void 0 : _a[entityId];\n    if (!entityState) {\n        // Entity doesn't exist - return minimal context\n        return {\n            entity: entityId,\n            state: 'unavailable',\n            name: (config === null || config === void 0 ? void 0 : config.name) || entityId,\n            attributes: {},\n            unit: '',\n            domain: entityId.split('.')[0] || 'unknown',\n            device_class: '',\n            friendly_name: (config === null || config === void 0 ? void 0 : config.name) || entityId,\n            config: config || {},\n        };\n    }\n    const domain = entityId.split('.')[0];\n    const attributes = entityState.attributes || {};\n    // Build context object\n    const context = {\n        // Primary entity reference (like Mushroom Card)\n        entity: entityId,\n        // Current state value\n        state: entityState.state,\n        // Name (custom name or friendly name)\n        name: (config === null || config === void 0 ? void 0 : config.name) || attributes.friendly_name || entityId,\n        // All entity attributes\n        attributes: attributes,\n        // Convenience accessors\n        unit: attributes.unit_of_measurement || '',\n        domain: domain,\n        device_class: attributes.device_class || '',\n        friendly_name: attributes.friendly_name || '',\n        // Config access (icon config, entity config, etc.)\n        config: config || {},\n        // Computed helpers\n        state_number: parseFloat(entityState.state),\n        state_boolean: entityState.state === 'on' || entityState.state === 'true' || entityState.state === 'yes',\n    };\n    return context;\n}\n/**\n * Build context for multiple entities (for modules with multiple entity configs)\n * @param entities Array of entity IDs\n * @param hass Home Assistant instance\n * @param configs Optional array of config objects matching entity array\n * @returns Context object with array of entity contexts\n */\nfunction buildMultiEntityContext(entities, hass, configs) {\n    const entityContexts = entities.map((entityId, index) => {\n        const config = configs === null || configs === void 0 ? void 0 : configs[index];\n        return buildEntityContext(entityId, hass, config);\n    });\n    // Return first entity as primary context + entities array\n    const primaryContext = entityContexts[0] || {\n        entity: '',\n        state: 'unavailable',\n        name: '',\n        attributes: {},\n        unit: '',\n        domain: 'unknown',\n        device_class: '',\n        friendly_name: '',\n        config: {},\n    };\n    return Object.assign(Object.assign({}, primaryContext), { entities: entityContexts });\n}\n/**\n * Check if a template result is valid JSON\n * @param result Template result string\n * @returns True if valid JSON\n */\nfunction isJsonResult(result) {\n    if (!result || typeof result !== 'string')\n        return false;\n    const trimmed = result.trim();\n    return (trimmed.startsWith('{') && trimmed.endsWith('}')) ||\n        (trimmed.startsWith('[') && trimmed.endsWith(']'));\n}\n/**\n * Safely parse JSON template result\n * @param result Template result string\n * @returns Parsed object or null if invalid\n */\nfunction parseJsonResult(result) {\n    if (!isJsonResult(result))\n        return null;\n    try {\n        return JSON.parse(result.trim());\n    }\n    catch (error) {\n        console.warn('[UltraCard] Failed to parse JSON template result:', error);\n        return null;\n    }\n}\n\n\n//# sourceURL=webpack://ultra-card/./src/utils/template-context.ts?");

/***/ }),

/***/ "./src/utils/template-parser.ts":
/*!**************************************!*\
  !*** ./src/utils/template-parser.ts ***!
  \**************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   getTemplateError: () => (/* binding */ getTemplateError),\n/* harmony export */   hasTemplateError: () => (/* binding */ hasTemplateError),\n/* harmony export */   isStringResult: () => (/* binding */ isStringResult),\n/* harmony export */   parseUnifiedTemplate: () => (/* binding */ parseUnifiedTemplate),\n/* harmony export */   validateUnifiedTemplate: () => (/* binding */ validateUnifiedTemplate)\n/* harmony export */ });\n/**\n * Unified Template Parser\n * Handles parsing of unified template results (string or JSON)\n * Validates JSON structure and provides helpful error messages\n */\n/**\n * Parse unified template result\n * Supports both simple string returns and JSON object returns\n * @param templateResult The raw template result from HA\n * @returns Parsed result object with display properties\n */\nfunction parseUnifiedTemplate(templateResult) {\n    if (templateResult === undefined || templateResult === null) {\n        return { _error: 'Template returned undefined or null' };\n    }\n    // If Home Assistant returns an object directly (not a string), use it as-is\n    if (typeof templateResult === 'object' && !Array.isArray(templateResult)) {\n        // Sanitize and validate properties\n        const result = {};\n        // Icon module properties - trim all string values to handle whitespace from Jinja templates\n        if (templateResult.icon !== undefined)\n            result.icon = String(templateResult.icon).trim();\n        if (templateResult.icon_color !== undefined)\n            result.icon_color = String(templateResult.icon_color).trim();\n        if (templateResult.container_background_color !== undefined)\n            result.container_background_color = String(templateResult.container_background_color).trim();\n        if (templateResult.name !== undefined)\n            result.name = String(templateResult.name).trim();\n        if (templateResult.name_color !== undefined)\n            result.name_color = String(templateResult.name_color).trim();\n        if (templateResult.state_text !== undefined)\n            result.state_text = String(templateResult.state_text).trim();\n        if (templateResult.state_color !== undefined)\n            result.state_color = String(templateResult.state_color).trim();\n        // Text/content module properties\n        if (templateResult.content !== undefined)\n            result.content = String(templateResult.content).trim();\n        if (templateResult.color !== undefined)\n            result.color = String(templateResult.color).trim();\n        // Bar module properties\n        if (templateResult.value !== undefined)\n            result.value = templateResult.value;\n        if (templateResult.label !== undefined)\n            result.label = String(templateResult.label).trim();\n        // Gauge module properties\n        if (templateResult.gauge_color !== undefined)\n            result.gauge_color = String(templateResult.gauge_color).trim();\n        // Graphs module properties\n        if (templateResult.colors !== undefined && Array.isArray(templateResult.colors)) {\n            result.colors = templateResult.colors.map(c => String(c).trim());\n        }\n        if (templateResult.global_color !== undefined)\n            result.global_color = String(templateResult.global_color).trim();\n        if (templateResult.fill_area !== undefined)\n            result.fill_area = Boolean(templateResult.fill_area);\n        if (templateResult.pie_fill !== undefined) {\n            const pieFill = typeof templateResult.pie_fill === 'number'\n                ? templateResult.pie_fill\n                : parseFloat(String(templateResult.pie_fill));\n            if (!isNaN(pieFill))\n                result.pie_fill = pieFill;\n        }\n        // Spinbox module properties\n        if (templateResult.button_background_color !== undefined)\n            result.button_background_color = String(templateResult.button_background_color).trim();\n        if (templateResult.button_text_color !== undefined)\n            result.button_text_color = String(templateResult.button_text_color).trim();\n        if (templateResult.value_color !== undefined)\n            result.value_color = String(templateResult.value_color).trim();\n        // Camera module properties\n        if (templateResult.entity !== undefined)\n            result.entity = String(templateResult.entity).trim();\n        if (templateResult.visible !== undefined)\n            result.visible = Boolean(templateResult.visible);\n        if (templateResult.overlay_text !== undefined)\n            result.overlay_text = String(templateResult.overlay_text).trim();\n        if (templateResult.overlay_color !== undefined)\n            result.overlay_color = String(templateResult.overlay_color).trim();\n        return result;\n    }\n    const resultStr = String(templateResult).trim();\n    if (resultStr === '') {\n        return { _error: 'Template returned empty string' };\n    }\n    // Check if result is JSON string\n    if ((resultStr.startsWith('{') && resultStr.endsWith('}')) ||\n        (resultStr.startsWith('[') && resultStr.endsWith(']'))) {\n        try {\n            const parsed = JSON.parse(resultStr);\n            // Validate it's an object (not array)\n            if (typeof parsed !== 'object' || Array.isArray(parsed)) {\n                return {\n                    _error: 'Template must return an object, not an array',\n                };\n            }\n            // Sanitize and validate properties - trim all string values to handle whitespace from Jinja templates\n            const result = {};\n            // Icon module properties\n            if (parsed.icon !== undefined)\n                result.icon = String(parsed.icon).trim();\n            if (parsed.icon_color !== undefined)\n                result.icon_color = String(parsed.icon_color).trim();\n            if (parsed.container_background_color !== undefined)\n                result.container_background_color = String(parsed.container_background_color).trim();\n            if (parsed.name !== undefined)\n                result.name = String(parsed.name).trim();\n            if (parsed.name_color !== undefined)\n                result.name_color = String(parsed.name_color).trim();\n            if (parsed.state_text !== undefined)\n                result.state_text = String(parsed.state_text).trim();\n            if (parsed.state_color !== undefined)\n                result.state_color = String(parsed.state_color).trim();\n            // Text/content module properties\n            if (parsed.content !== undefined)\n                result.content = String(parsed.content).trim();\n            if (parsed.color !== undefined)\n                result.color = String(parsed.color).trim();\n            // Bar module properties\n            if (parsed.value !== undefined)\n                result.value = parsed.value;\n            if (parsed.label !== undefined)\n                result.label = String(parsed.label).trim();\n            // Gauge module properties\n            if (parsed.gauge_color !== undefined)\n                result.gauge_color = String(parsed.gauge_color).trim();\n            // Graphs module properties\n            if (parsed.colors !== undefined && Array.isArray(parsed.colors)) {\n                result.colors = parsed.colors.map(c => String(c).trim());\n            }\n            if (parsed.global_color !== undefined)\n                result.global_color = String(parsed.global_color).trim();\n            if (parsed.fill_area !== undefined)\n                result.fill_area = Boolean(parsed.fill_area);\n            if (parsed.pie_fill !== undefined) {\n                const pieFill = typeof parsed.pie_fill === 'number'\n                    ? parsed.pie_fill\n                    : parseFloat(String(parsed.pie_fill));\n                if (!isNaN(pieFill))\n                    result.pie_fill = pieFill;\n            }\n            // Spinbox module properties\n            if (parsed.button_background_color !== undefined)\n                result.button_background_color = String(parsed.button_background_color).trim();\n            if (parsed.button_text_color !== undefined)\n                result.button_text_color = String(parsed.button_text_color).trim();\n            if (parsed.value_color !== undefined)\n                result.value_color = String(parsed.value_color).trim();\n            // Camera module properties\n            if (parsed.entity !== undefined)\n                result.entity = String(parsed.entity).trim();\n            if (parsed.visible !== undefined)\n                result.visible = Boolean(parsed.visible);\n            if (parsed.overlay_text !== undefined)\n                result.overlay_text = String(parsed.overlay_text).trim();\n            if (parsed.overlay_color !== undefined)\n                result.overlay_color = String(parsed.overlay_color).trim();\n            return result;\n        }\n        catch (error) {\n            return {\n                _error: `Invalid JSON: ${error instanceof Error ? error.message : 'Unknown error'}`,\n            };\n        }\n    }\n    // Simple string return - treat as icon name (backward compatible)\n    return {\n        icon: resultStr,\n        _isString: true,\n    };\n}\n/**\n * Check if a template result has errors\n * @param result Parsed template result\n * @returns True if result has errors\n */\nfunction hasTemplateError(result) {\n    return result._error !== undefined;\n}\n/**\n * Get error message from template result\n * @param result Parsed template result\n * @returns Error message or null\n */\nfunction getTemplateError(result) {\n    return result._error || null;\n}\n/**\n * Check if result was a simple string (not JSON)\n * @param result Parsed template result\n * @returns True if original result was a string\n */\nfunction isStringResult(result) {\n    return result._isString === true;\n}\n/**\n * Validate unified template syntax (basic checks before evaluation)\n * @param template Template string\n * @returns Validation result with errors if any\n */\nfunction validateUnifiedTemplate(template) {\n    const errors = [];\n    if (!template || template.trim() === '') {\n        errors.push('Template is empty');\n        return { valid: false, errors };\n    }\n    const trimmed = template.trim();\n    // Check for unclosed Jinja2 tags\n    const openBraces = (trimmed.match(/\\{\\{/g) || []).length;\n    const closeBraces = (trimmed.match(/\\}\\}/g) || []).length;\n    if (openBraces !== closeBraces) {\n        errors.push('Unclosed {{ }} template expression');\n    }\n    const openStatements = (trimmed.match(/\\{%/g) || []).length;\n    const closeStatements = (trimmed.match(/%\\}/g) || []).length;\n    if (openStatements !== closeStatements) {\n        errors.push('Unclosed {% %} template statement');\n    }\n    // Check for common mistakes\n    if (trimmed.includes('{{states.') && !trimmed.includes('}}')) {\n        errors.push('Incomplete states access - missing }}');\n    }\n    return {\n        valid: errors.length === 0,\n        errors,\n    };\n}\n\n\n//# sourceURL=webpack://ultra-card/./src/utils/template-parser.ts?");

/***/ })

}]);