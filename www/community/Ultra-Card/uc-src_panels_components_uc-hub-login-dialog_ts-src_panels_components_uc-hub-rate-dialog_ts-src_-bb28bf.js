"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunkultra_card"] = self["webpackChunkultra_card"] || []).push([["src_panels_components_uc-hub-login-dialog_ts-src_panels_components_uc-hub-rate-dialog_ts-src_-bb28bf"],{

/***/ "./src/panels/components/uc-hub-login-dialog.ts":
/*!******************************************************!*\
  !*** ./src/panels/components/uc-hub-login-dialog.ts ***!
  \******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   UcHubLoginDialog: () => (/* binding */ UcHubLoginDialog)\n/* harmony export */ });\n/* harmony import */ var lit__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! lit */ \"./src/node_modules/lit/index.js\");\n/* harmony import */ var _services_uc_cloud_auth_service__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../services/uc-cloud-auth-service */ \"./src/services/uc-cloud-auth-service.ts\");\n/**\n * Ultra Card Hub — Login / Register dialog\n * Routes users to the Ultra Card Connect integration or the hub Account tab only.\n */\n\n\nclass UcHubLoginDialog extends lit__WEBPACK_IMPORTED_MODULE_0__.LitElement {\n    _close() {\n        this.dispatchEvent(new CustomEvent('close', {\n            bubbles: true,\n            composed: true,\n        }));\n    }\n    render() {\n        return (0,lit__WEBPACK_IMPORTED_MODULE_0__.html) `\n      <ha-dialog open @closed=${this._close} .heading=${'Sign in to Ultra Card'}>\n        <div class=\"login-dialog-content\">\n          <div class=\"login-section\">\n            <p class=\"login-section-title\">Connect with Ultra Card Connect</p>\n            <p class=\"login-section-desc\">\n              The fastest way to connect — installs in 30 seconds. Syncs across all your devices.\n            </p>\n            <a\n              class=\"login-cta-btn\"\n              href=\"/config/integrations/integration/ultra_card_pro_cloud\"\n              target=\"_top\"\n            >\n              <ha-icon icon=\"mdi:cog\"></ha-icon>\n              Configure Ultra Card Connect\n            </a>\n          </div>\n\n          <div class=\"login-divider\"></div>\n\n          <p class=\"login-note\">\n            Already have the integration? Open the <strong>Account</strong> tab in the Ultra Card hub to sign in or create an account.\n          </p>\n        </div>\n      </ha-dialog>\n    `;\n    }\n}\nUcHubLoginDialog.styles = (0,lit__WEBPACK_IMPORTED_MODULE_0__.css) `\n    .login-dialog-content {\n      padding: 8px 0;\n      display: flex;\n      flex-direction: column;\n      gap: 20px;\n    }\n\n    .login-section {\n      display: flex;\n      flex-direction: column;\n      gap: 10px;\n    }\n\n    .login-section-title {\n      font-size: 12px;\n      font-weight: 600;\n      text-transform: uppercase;\n      letter-spacing: 0.5px;\n      color: var(--secondary-text-color);\n      margin: 0 0 4px 0;\n    }\n\n    .login-section-desc {\n      font-size: 13px;\n      color: var(--secondary-text-color);\n      margin: 0 0 8px 0;\n      line-height: 1.4;\n    }\n\n    .login-cta-btn {\n      display: inline-flex;\n      align-items: center;\n      justify-content: center;\n      gap: 8px;\n      padding: 10px 16px;\n      border-radius: 8px;\n      border: none;\n      background: var(--primary-color);\n      color: var(--primary-text-color);\n      font-size: 14px;\n      font-weight: 500;\n      cursor: pointer;\n      text-decoration: none;\n      transition: opacity 0.15s ease;\n    }\n\n    .login-cta-btn:hover {\n      opacity: 0.9;\n    }\n\n    .login-cta-btn ha-icon {\n      --mdc-icon-size: 18px;\n    }\n\n    .login-divider {\n      height: 1px;\n      background: var(--divider-color, rgba(0, 0, 0, 0.08));\n      margin: 4px 0;\n    }\n\n    .login-note {\n      font-size: 13px;\n      color: var(--secondary-text-color);\n      line-height: 1.5;\n      margin: 0;\n    }\n\n    .login-note a {\n      color: var(--primary-color);\n      text-decoration: none;\n      font-weight: 500;\n    }\n\n    .login-note a:hover {\n      text-decoration: underline;\n    }\n  `;\n// Guard against double-registration: this component is shared between\n// ultra-card.js (main card bundle) and ultra-card-panel.js (sidebar panel bundle).\n// Without the guard, the second bundle to load throws a DOMException that crashes\n// the custom elements registry and shows \"Configuration error\" on every card.\nif (!customElements.get('uc-hub-login-dialog')) {\n    customElements.define('uc-hub-login-dialog', UcHubLoginDialog);\n}\n\n\n//# sourceURL=webpack://ultra-card/./src/panels/components/uc-hub-login-dialog.ts?");

/***/ }),

/***/ "./src/panels/components/uc-hub-rate-dialog.ts":
/*!*****************************************************!*\
  !*** ./src/panels/components/uc-hub-rate-dialog.ts ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   UcHubRateDialog: () => (/* binding */ UcHubRateDialog)\n/* harmony export */ });\n/* harmony import */ var lit__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! lit */ \"./src/node_modules/lit/index.js\");\n/* harmony import */ var lit_decorators_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! lit/decorators.js */ \"./src/node_modules/lit/decorators.js\");\n/* harmony import */ var _services_uc_cloud_sync_service__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../services/uc-cloud-sync-service */ \"./src/services/uc-cloud-sync-service.ts\");\n/**\n * Ultra Card Hub — Rate preset dialog\n * Uses a fully custom overlay (no ha-dialog) so it always renders above\n * the builder's module-selector-popup (z-index 1000).\n */\nvar __decorate = (undefined && undefined.__decorate) || function (decorators, target, key, desc) {\n    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;\n    if (typeof Reflect === \"object\" && typeof Reflect.decorate === \"function\") r = Reflect.decorate(decorators, target, key, desc);\n    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;\n    return c > 3 && r && Object.defineProperty(target, key, r), r;\n};\n\n\n\nclass UcHubRateDialog extends lit__WEBPACK_IMPORTED_MODULE_0__.LitElement {\n    constructor() {\n        super(...arguments);\n        this.presetId = '';\n        this.presetName = '';\n        this.existingRating = 0;\n        this._selectedRating = 0;\n        this._hoveredRating = 0;\n        this._submitting = false;\n        this._error = '';\n    }\n    willUpdate(changed) {\n        if (changed.has('existingRating') || changed.has('presetId')) {\n            if (this._selectedRating === 0 && this.existingRating > 0) {\n                this._selectedRating = this.existingRating;\n            }\n        }\n    }\n    _close() {\n        this._selectedRating = 0;\n        this._hoveredRating = 0;\n        this._error = '';\n        this.dispatchEvent(new CustomEvent('close', { bubbles: true, composed: true }));\n    }\n    _displayRating() {\n        return this._hoveredRating > 0 ? this._hoveredRating : this._selectedRating;\n    }\n    _ratingLabel(r) {\n        var _a;\n        return (_a = ['', 'Poor', 'Fair', 'Good', 'Great', 'Excellent'][r]) !== null && _a !== void 0 ? _a : '';\n    }\n    async _handleSubmit() {\n        if (this._selectedRating < 1 || this._submitting || !this.presetId)\n            return;\n        this._submitting = true;\n        this._error = '';\n        try {\n            await _services_uc_cloud_sync_service__WEBPACK_IMPORTED_MODULE_2__.ucCloudSyncService.submitReview(this.presetId, this._selectedRating);\n            this.dispatchEvent(new CustomEvent('rating-submitted', {\n                detail: { presetId: this.presetId, rating: this._selectedRating },\n                bubbles: true,\n                composed: true,\n            }));\n            this._close();\n        }\n        catch (err) {\n            this._error = err instanceof Error ? err.message : 'Failed to submit rating';\n        }\n        finally {\n            this._submitting = false;\n        }\n    }\n    render() {\n        const displayRating = this._displayRating();\n        return (0,lit__WEBPACK_IMPORTED_MODULE_0__.html) `\n      <div class=\"overlay\" @click=${(e) => { if (e.target === e.currentTarget)\n            this._close(); }}>\n        <div class=\"dialog-surface\">\n\n          <div class=\"dialog-header\">\n            <h2 class=\"dialog-title\">Rate this preset</h2>\n            <button class=\"dialog-close\" @click=${this._close} ?disabled=${this._submitting}>\n              <ha-icon icon=\"mdi:close\" style=\"--mdc-icon-size:20px;\"></ha-icon>\n            </button>\n          </div>\n\n          ${this.presetName\n            ? (0,lit__WEBPACK_IMPORTED_MODULE_0__.html) `<p class=\"rate-dialog-subtitle\">${this.presetName}</p>`\n            : ''}\n\n          <div class=\"rate-stars-row\" @mouseleave=${() => (this._hoveredRating = 0)}>\n            ${[1, 2, 3, 4, 5].map(star => (0,lit__WEBPACK_IMPORTED_MODULE_0__.html) `\n              <ha-icon\n                icon=${star <= displayRating ? 'mdi:star' : 'mdi:star-outline'}\n                class=${star <= displayRating ? '' : 'empty'}\n                @mouseenter=${() => (this._hoveredRating = star)}\n                @click=${() => (this._selectedRating = star)}\n              ></ha-icon>\n            `)}\n          </div>\n\n          <p class=\"rate-hint\">${this._ratingLabel(displayRating)}</p>\n\n          ${this._error ? (0,lit__WEBPACK_IMPORTED_MODULE_0__.html) `<p class=\"rate-error\">${this._error}</p>` : ''}\n\n          <div class=\"rate-actions\">\n            <button\n              class=\"rate-btn rate-btn-submit\"\n              ?disabled=${this._selectedRating < 1 || this._submitting}\n              @click=${this._handleSubmit}\n            >\n              ${this._submitting\n            ? (0,lit__WEBPACK_IMPORTED_MODULE_0__.html) `<ha-icon icon=\"mdi:loading\" class=\"rate-spin\"></ha-icon> Submitting…`\n            : this.existingRating > 0\n                ? 'Update rating'\n                : 'Submit rating'}\n            </button>\n            <button class=\"rate-btn rate-btn-cancel\" @click=${this._close} ?disabled=${this._submitting}>\n              Cancel\n            </button>\n          </div>\n\n        </div>\n      </div>\n    `;\n    }\n}\nUcHubRateDialog.styles = (0,lit__WEBPACK_IMPORTED_MODULE_0__.css) `\n    /* Full-screen backdrop — position: fixed so it escapes any parent stacking context */\n    .overlay {\n      position: fixed;\n      inset: 0;\n      z-index: 9999;\n      display: flex;\n      align-items: center;\n      justify-content: center;\n      background: rgba(0, 0, 0, 0.55);\n      animation: fadeIn 0.15s ease;\n    }\n\n    @keyframes fadeIn {\n      from { opacity: 0; }\n      to   { opacity: 1; }\n    }\n\n    .dialog-surface {\n      background: var(--ha-card-background, var(--card-background-color, #1c1c1e));\n      border-radius: 16px;\n      padding: 24px 28px;\n      min-width: 300px;\n      max-width: min(480px, 90vw);\n      box-shadow: 0 8px 40px rgba(0, 0, 0, 0.5);\n      animation: slideUp 0.18s ease;\n    }\n\n    @keyframes slideUp {\n      from { transform: translateY(12px); opacity: 0; }\n      to   { transform: translateY(0);    opacity: 1; }\n    }\n\n    .dialog-header {\n      display: flex;\n      align-items: center;\n      justify-content: space-between;\n      margin-bottom: 16px;\n    }\n\n    .dialog-title {\n      font-size: 17px;\n      font-weight: 600;\n      color: var(--primary-text-color);\n      margin: 0;\n    }\n\n    .dialog-close {\n      background: none;\n      border: none;\n      cursor: pointer;\n      color: var(--secondary-text-color);\n      padding: 4px;\n      border-radius: 6px;\n      display: flex;\n      align-items: center;\n      transition: background 0.15s;\n    }\n\n    .dialog-close:hover {\n      background: rgba(var(--rgb-primary-text-color, 255, 255, 255), 0.08);\n    }\n\n    .rate-dialog-subtitle {\n      font-size: 13px;\n      color: var(--secondary-text-color);\n      margin: 0 0 20px;\n    }\n\n    .rate-stars-row {\n      display: flex;\n      align-items: center;\n      gap: 6px;\n      cursor: pointer;\n      user-select: none;\n      margin-bottom: 8px;\n    }\n\n    .rate-stars-row ha-icon {\n      --mdc-icon-size: 36px;\n      color: #ffb300;\n      transition: transform 0.1s ease;\n    }\n\n    .rate-stars-row ha-icon.empty {\n      color: var(--secondary-text-color);\n      opacity: 0.35;\n    }\n\n    .rate-stars-row ha-icon:hover {\n      transform: scale(1.15);\n    }\n\n    .rate-hint {\n      font-size: 12px;\n      color: var(--secondary-text-color);\n      margin-bottom: 20px;\n      min-height: 18px;\n    }\n\n    .rate-actions {\n      display: flex;\n      gap: 10px;\n    }\n\n    .rate-btn {\n      flex: 1;\n      display: inline-flex;\n      align-items: center;\n      justify-content: center;\n      gap: 6px;\n      padding: 9px 16px;\n      border-radius: 8px;\n      border: none;\n      font-size: 14px;\n      font-weight: 500;\n      cursor: pointer;\n      transition: opacity 0.15s ease, background 0.15s ease;\n    }\n\n    .rate-btn-submit {\n      background: var(--primary-color);\n      color: white;\n    }\n\n    .rate-btn-submit:hover:not(:disabled) {\n      opacity: 0.88;\n    }\n\n    .rate-btn-submit:disabled {\n      opacity: 0.4;\n      cursor: not-allowed;\n    }\n\n    .rate-btn-cancel {\n      background: transparent;\n      color: var(--secondary-text-color);\n      border: 1px solid var(--divider-color, rgba(255,255,255,0.12));\n    }\n\n    .rate-btn-cancel:hover:not(:disabled) {\n      background: rgba(var(--rgb-primary-text-color, 255,255,255), 0.06);\n    }\n\n    .rate-error {\n      font-size: 12px;\n      color: var(--error-color, #db4437);\n      margin: 4px 0 0;\n    }\n\n    @keyframes spin {\n      to { transform: rotate(360deg); }\n    }\n\n    .rate-spin {\n      animation: spin 0.8s linear infinite;\n    }\n  `;\n__decorate([\n    (0,lit_decorators_js__WEBPACK_IMPORTED_MODULE_1__.property)()\n], UcHubRateDialog.prototype, \"presetId\", void 0);\n__decorate([\n    (0,lit_decorators_js__WEBPACK_IMPORTED_MODULE_1__.property)()\n], UcHubRateDialog.prototype, \"presetName\", void 0);\n__decorate([\n    (0,lit_decorators_js__WEBPACK_IMPORTED_MODULE_1__.property)({ type: Number })\n], UcHubRateDialog.prototype, \"existingRating\", void 0);\n__decorate([\n    (0,lit_decorators_js__WEBPACK_IMPORTED_MODULE_1__.state)()\n], UcHubRateDialog.prototype, \"_selectedRating\", void 0);\n__decorate([\n    (0,lit_decorators_js__WEBPACK_IMPORTED_MODULE_1__.state)()\n], UcHubRateDialog.prototype, \"_hoveredRating\", void 0);\n__decorate([\n    (0,lit_decorators_js__WEBPACK_IMPORTED_MODULE_1__.state)()\n], UcHubRateDialog.prototype, \"_submitting\", void 0);\n__decorate([\n    (0,lit_decorators_js__WEBPACK_IMPORTED_MODULE_1__.state)()\n], UcHubRateDialog.prototype, \"_error\", void 0);\nif (!customElements.get('uc-hub-rate-dialog')) {\n    customElements.define('uc-hub-rate-dialog', UcHubRateDialog);\n}\n\n\n//# sourceURL=webpack://ultra-card/./src/panels/components/uc-hub-rate-dialog.ts?");

/***/ }),

/***/ "./src/services/directories-pro-presets-api.ts":
/*!*****************************************************!*\
  !*** ./src/services/directories-pro-presets-api.ts ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   DirectoriesProPresetsAPI: () => (/* binding */ DirectoriesProPresetsAPI),\n/* harmony export */   directoriesProPresetsAPI: () => (/* binding */ directoriesProPresetsAPI)\n/* harmony export */ });\n/**\n * Directories Pro Presets API Service\n * Fetches presets from ultracard.io WordPress site using Directories Pro\n */\nclass DirectoriesProPresetsAPI {\n    constructor() {\n        this.cache = new Map();\n        this.localStorageDisabled = false; // Flag to disable localStorage if quota keeps failing\n        this.corsProxies = [\n            'https://api.allorigins.win/raw?url=',\n            'https://corsproxy.io/?',\n            'https://cors-anywhere.herokuapp.com/',\n        ];\n    }\n    /**\n     * Fetch all presets from Directories Pro\n     */\n    async fetchPresets(params = {}) {\n        const cacheKey = `presets_${JSON.stringify(params)}`;\n        // Check memory cache first\n        const cached = this.cache.get(cacheKey);\n        if (cached && Date.now() - cached.timestamp < DirectoriesProPresetsAPI.CACHE_DURATION) {\n            return cached.data;\n        }\n        // Check localStorage cache\n        const localCached = this._getFromLocalStorage(cacheKey);\n        if (localCached) {\n            this.cache.set(cacheKey, localCached);\n            return localCached.data;\n        }\n        try {\n            const queryParams = new URLSearchParams();\n            // Add pagination\n            if (params.page)\n                queryParams.set('page', params.page.toString());\n            if (params.per_page)\n                queryParams.set('per_page', params.per_page.toString());\n            // Add filtering\n            if (params.category)\n                queryParams.set('category', params.category);\n            if (params.search)\n                queryParams.set('search', params.search);\n            if (params.sort)\n                queryParams.set('sort', params.sort);\n            // Embed author and media information\n            queryParams.set('_embed', 'true');\n            // Use Directories Pro post type\n            const url = `${DirectoriesProPresetsAPI.API_BASE}/presets_dir_ltg?${queryParams.toString()}`;\n            // Try multiple methods to fetch data\n            const posts = await this._fetchWithCorsResilience(url);\n            // Convert Directories Pro presets to our preset format\n            const data = {\n                presets: Array.isArray(posts)\n                    ? posts.map((post) => {\n                        var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r, _s, _t, _u, _v, _w;\n                        const meta = post.preset_meta || {};\n                        const tags = meta.tags ? meta.tags.split(',').map((tag) => tag.trim()) : [];\n                        const integrations = meta.integrations\n                            ? String(meta.integrations)\n                                .split(',')\n                                .map((t) => t.trim())\n                                .filter(Boolean)\n                            : [];\n                        // Plain-text version for the truncated preview line\n                        const fullDescription = this._stripHtml(meta.description ||\n                            ((_a = post.excerpt) === null || _a === void 0 ? void 0 : _a.rendered) ||\n                            ((_b = post.content) === null || _b === void 0 ? void 0 : _b.rendered) ||\n                            'No description available');\n                        // HTML version for Read More — prefer WP REST rendered HTML, fall back to PHP-safe HTML\n                        const fullDescriptionHtml = this._ensureHtml(((_c = post.content) === null || _c === void 0 ? void 0 : _c.rendered) ||\n                            ((_d = post.excerpt) === null || _d === void 0 ? void 0 : _d.rendered) ||\n                            meta.description_html ||\n                            fullDescription);\n                        // Extract featured image from embedded media\n                        const featuredImageUrl = ((_g = (_f = (_e = post._embedded) === null || _e === void 0 ? void 0 : _e['wp:featuredmedia']) === null || _f === void 0 ? void 0 : _f[0]) === null || _g === void 0 ? void 0 : _g.source_url) ||\n                            meta.featured_image ||\n                            post.featured_media_src_url ||\n                            '';\n                        return {\n                            id: post.id,\n                            name: this._decodeHtmlEntities(((_h = post.title) === null || _h === void 0 ? void 0 : _h.rendered) || 'Untitled Preset'),\n                            description: this._truncateDescription(fullDescription, 15), // Truncated for cards\n                            description_full: fullDescriptionHtml, // HTML for Read More\n                            shortcode: meta.shortcode || '{\"rows\":[]}',\n                            category: meta.category || 'badges', // Default to badges instead of custom\n                            tags: tags,\n                            integrations,\n                            author: ((_l = (_k = (_j = post._embedded) === null || _j === void 0 ? void 0 : _j.author) === null || _k === void 0 ? void 0 : _k[0]) === null || _l === void 0 ? void 0 : _l.display_name) ||\n                                ((_p = (_o = (_m = post._embedded) === null || _m === void 0 ? void 0 : _m.author) === null || _o === void 0 ? void 0 : _o[0]) === null || _p === void 0 ? void 0 : _p.name) ||\n                                ((_s = (_r = (_q = post._embedded) === null || _q === void 0 ? void 0 : _q.author) === null || _r === void 0 ? void 0 : _r[0]) === null || _s === void 0 ? void 0 : _s.slug) ||\n                                'Community',\n                            author_avatar: ((_w = (_v = (_u = (_t = post._embedded) === null || _t === void 0 ? void 0 : _t.author) === null || _u === void 0 ? void 0 : _u[0]) === null || _v === void 0 ? void 0 : _v.avatar_urls) === null || _w === void 0 ? void 0 : _w['48']) || '',\n                            downloads: parseInt(meta.downloads) || 0,\n                            rating: parseFloat(meta.rating) || 0,\n                            reviews_count: parseInt(meta.rating_count) || 0,\n                            rating_count: parseInt(meta.rating_count) || 0,\n                            created: post.date,\n                            updated: post.modified,\n                            is_featured: false,\n                            difficulty: meta.difficulty || 'beginner',\n                            compatibility: meta.compatibility ? meta.compatibility.split(',') : [],\n                            featured_image: featuredImageUrl,\n                            gallery: Array.isArray(meta.gallery) && meta.gallery.length > 0 ? meta.gallery : [],\n                            preset_url: post.link || `https://ultracard.io/presets/${post.slug}/`,\n                        };\n                    })\n                    : [],\n                total: posts.length || 0,\n                pages: 1,\n                current_page: 1,\n            };\n            // Validate response structure\n            if (!Array.isArray(posts)) {\n                throw new Error('Invalid response format: expected array of posts');\n            }\n            // Cache the successful response\n            const cacheData = { data, timestamp: Date.now() };\n            this.cache.set(cacheKey, cacheData);\n            this._saveToLocalStorage(cacheKey, cacheData);\n            // Successfully fetched presets (silent)\n            return data;\n        }\n        catch (error) {\n            console.error('Failed to fetch Directories Pro presets:', error);\n            // Try to return stale cache if available (extended duration for CORS failures)\n            const staleCache = this._getFromLocalStorage(cacheKey, true);\n            if (staleCache) {\n                console.warn('Using stale cached presets due to fetch error');\n                return staleCache.data;\n            }\n            // Try to get any cached presets from any previous successful calls\n            const fallbackCache = this._getFallbackCache();\n            if (fallbackCache) {\n                console.warn('Using fallback cached presets due to fetch error');\n                return fallbackCache;\n            }\n            // Return empty response as final fallback\n            return {\n                presets: [],\n                total: 0,\n                pages: 0,\n                current_page: 1,\n            };\n        }\n    }\n    /**\n     * Fetch a single preset by ID from Directories Pro\n     */\n    async fetchPreset(id) {\n        var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r, _s, _t;\n        const cacheKey = `preset_${id}`;\n        // Check cache first\n        const cached = this.cache.get(cacheKey);\n        if (cached && Date.now() - cached.timestamp < DirectoriesProPresetsAPI.CACHE_DURATION) {\n            return cached.data;\n        }\n        try {\n            const url = `${DirectoriesProPresetsAPI.API_BASE}/presets_dir_ltg/${id}?_embed=true`;\n            const post = await this._fetchWithCorsResilience(url);\n            const meta = post.preset_meta || {};\n            const tags = meta.tags ? meta.tags.split(',').map((tag) => tag.trim()) : [];\n            const integrations = meta.integrations\n                ? String(meta.integrations)\n                    .split(',')\n                    .map((t) => t.trim())\n                    .filter(Boolean)\n                : [];\n            // Plain-text version for the truncated preview line\n            const fullDescription = this._stripHtml(((_a = post.excerpt) === null || _a === void 0 ? void 0 : _a.rendered) || ((_b = post.content) === null || _b === void 0 ? void 0 : _b.rendered) || 'No description available');\n            // HTML version for Read More — prefer WP REST rendered HTML, fall back to PHP-safe HTML\n            const fullDescriptionHtml = this._ensureHtml(((_c = post.content) === null || _c === void 0 ? void 0 : _c.rendered) ||\n                ((_d = post.excerpt) === null || _d === void 0 ? void 0 : _d.rendered) ||\n                meta.description_html ||\n                fullDescription);\n            const preset = {\n                id: post.id,\n                name: this._decodeHtmlEntities(((_e = post.title) === null || _e === void 0 ? void 0 : _e.rendered) || 'Untitled Preset'),\n                description: this._truncateDescription(fullDescription, 15),\n                description_full: fullDescriptionHtml,\n                shortcode: meta.shortcode || '{\"rows\":[]}',\n                category: meta.category || 'badges',\n                tags: tags,\n                integrations,\n                author: ((_h = (_g = (_f = post._embedded) === null || _f === void 0 ? void 0 : _f.author) === null || _g === void 0 ? void 0 : _g[0]) === null || _h === void 0 ? void 0 : _h.display_name) ||\n                    ((_l = (_k = (_j = post._embedded) === null || _j === void 0 ? void 0 : _j.author) === null || _k === void 0 ? void 0 : _k[0]) === null || _l === void 0 ? void 0 : _l.name) ||\n                    ((_p = (_o = (_m = post._embedded) === null || _m === void 0 ? void 0 : _m.author) === null || _o === void 0 ? void 0 : _o[0]) === null || _p === void 0 ? void 0 : _p.slug) ||\n                    'Community',\n                author_avatar: ((_t = (_s = (_r = (_q = post._embedded) === null || _q === void 0 ? void 0 : _q.author) === null || _r === void 0 ? void 0 : _r[0]) === null || _s === void 0 ? void 0 : _s.avatar_urls) === null || _t === void 0 ? void 0 : _t['48']) || '',\n                downloads: parseInt(meta.downloads) || 0,\n                rating: parseFloat(meta.rating) || 0,\n                reviews_count: parseInt(meta.rating_count) || 0,\n                rating_count: parseInt(meta.rating_count) || 0,\n                created: post.date,\n                updated: post.modified,\n                is_featured: false,\n                difficulty: meta.difficulty || 'beginner',\n                compatibility: meta.compatibility ? meta.compatibility.split(',') : [],\n                featured_image: meta.featured_image || post.featured_media_src_url || '',\n                gallery: Array.isArray(meta.gallery) ? meta.gallery : [],\n                preset_url: post.link || `https://ultracard.io/presets/${post.slug}/`,\n            };\n            // Cache the result\n            const cacheData = { data: preset, timestamp: Date.now() };\n            this.cache.set(cacheKey, cacheData);\n            return preset;\n        }\n        catch (error) {\n            console.error(`Failed to fetch preset ${id}:`, error);\n            return null;\n        }\n    }\n    /**\n     * Track preset download by incrementing the downloads meta field.\n     * Calls a custom REST endpoint on the WordPress site.\n     */\n    async trackDownload(presetId) {\n        try {\n            const url = `${DirectoriesProPresetsAPI.API_BASE.replace('/wp/v2', '/ultra-card/v1')}/presets/${presetId}/track-download`;\n            await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' } });\n        }\n        catch (error) {\n            console.warn(`Failed to track download for preset ${presetId}:`, error);\n        }\n    }\n    /**\n     * Get available categories from Directories Pro\n     */\n    async getCategories() {\n        const cacheKey = 'categories';\n        const cached = this.cache.get(cacheKey);\n        if (cached && Date.now() - cached.timestamp < DirectoriesProPresetsAPI.CACHE_DURATION) {\n            return cached.data;\n        }\n        try {\n            const response = await fetch(`${DirectoriesProPresetsAPI.API_BASE}/categories`, {\n                signal: AbortSignal.timeout(5000),\n            });\n            if (response.ok) {\n                const categories = await response.json();\n                const cacheData = { data: categories, timestamp: Date.now() };\n                this.cache.set(cacheKey, cacheData);\n                return categories;\n            }\n        }\n        catch (error) {\n            console.warn('Failed to fetch categories:', error);\n        }\n        // Fallback categories\n        return ['badges', 'layouts', 'widgets', 'dashboards', 'themes'];\n    }\n    /**\n     * Clear all caches\n     */\n    clearCache() {\n        this.cache.clear();\n        // Clear localStorage cache\n        const keys = Object.keys(localStorage);\n        keys.forEach(key => {\n            if (key.startsWith('wp-presets-cache-')) {\n                localStorage.removeItem(key);\n            }\n        });\n    }\n    /**\n     * Test CORS-resilient connection (for debugging)\n     */\n    async testConnection() {\n        const testUrl = `${DirectoriesProPresetsAPI.API_BASE}/presets_dir_ltg?per_page=1`;\n        const results = [];\n        // Test direct fetch\n        try {\n            await fetch(testUrl, {\n                method: 'GET',\n                headers: { Accept: 'application/json' },\n                signal: AbortSignal.timeout(5000),\n            });\n            results.push({ method: 'Direct Fetch', success: true });\n        }\n        catch (error) {\n            results.push({\n                method: 'Direct Fetch',\n                success: false,\n                error: error instanceof Error ? error.message : 'Unknown error',\n            });\n        }\n        // Test each CORS proxy\n        for (const proxy of this.corsProxies) {\n            try {\n                const proxyUrl = `${proxy}${encodeURIComponent(testUrl)}`;\n                await fetch(proxyUrl, {\n                    method: 'GET',\n                    headers: { Accept: 'application/json' },\n                    signal: AbortSignal.timeout(8000),\n                });\n                results.push({ method: `Proxy: ${proxy}`, success: true });\n            }\n            catch (error) {\n                results.push({\n                    method: `Proxy: ${proxy}`,\n                    success: false,\n                    error: error instanceof Error ? error.message : 'Unknown error',\n                });\n            }\n        }\n        return results;\n    }\n    /**\n     * Get cache statistics\n     */\n    getCacheStats() {\n        const memoryEntries = this.cache.size;\n        const keys = Object.keys(localStorage);\n        const localStorageEntries = keys.filter(key => key.startsWith('wp-presets-cache-')).length;\n        return { memoryEntries, localStorageEntries };\n    }\n    /**\n     * Save data to localStorage with timestamp\n     */\n    _saveToLocalStorage(key, data) {\n        // Skip if localStorage is disabled due to persistent quota issues\n        if (this.localStorageDisabled) {\n            return;\n        }\n        try {\n            // Clean up expired entries before saving to prevent quota issues\n            this._cleanupExpiredCache();\n            // Check if data is too large before saving\n            const serialized = JSON.stringify(data);\n            const estimatedSize = new Blob([serialized]).size;\n            if (estimatedSize > DirectoriesProPresetsAPI.MAX_ENTRY_SIZE) {\n                // Entry too large, skip silently (still cached in memory)\n                return;\n            }\n            const storageKey = `wp-presets-cache-${key}`;\n            localStorage.setItem(storageKey, serialized);\n        }\n        catch (error) {\n            // If quota exceeded, try cleaning up more aggressively and retry\n            if (error instanceof DOMException && error.name === 'QuotaExceededError') {\n                // Clean up aggressively (silently, no user-facing warning)\n                this._clearAllPresetCache();\n                try {\n                    // Retry after cleanup\n                    const serialized = JSON.stringify(data);\n                    const estimatedSize = new Blob([serialized]).size;\n                    // Only retry if the entry isn't too large\n                    if (estimatedSize <= DirectoriesProPresetsAPI.MAX_ENTRY_SIZE) {\n                        const storageKey = `wp-presets-cache-${key}`;\n                        localStorage.setItem(storageKey, serialized);\n                    }\n                    // If too large, silently skip (already cached in memory)\n                }\n                catch (retryError) {\n                    // If still failing after clearing all preset cache, localStorage is likely full from other data\n                    // Disable localStorage caching for this session and rely on memory cache only\n                    this.localStorageDisabled = true;\n                    // Clear all preset cache entries one more time to free up what we can\n                    this._clearAllPresetCache();\n                }\n            }\n            else {\n                // Other errors (non-quota) - fail silently, memory cache will handle it\n            }\n        }\n    }\n    /**\n     * Get data from localStorage\n     */\n    _getFromLocalStorage(key, allowStale = false) {\n        // Skip if localStorage is disabled\n        if (this.localStorageDisabled) {\n            return null;\n        }\n        try {\n            const storageKey = `wp-presets-cache-${key}`;\n            const stored = localStorage.getItem(storageKey);\n            if (!stored)\n                return null;\n            const parsed = JSON.parse(stored);\n            // Check if cache is still valid (unless allowing stale)\n            const cacheAge = Date.now() - parsed.timestamp;\n            const maxAge = allowStale\n                ? DirectoriesProPresetsAPI.EXTENDED_CACHE_DURATION\n                : DirectoriesProPresetsAPI.CACHE_DURATION;\n            if (cacheAge > maxAge) {\n                if (!allowStale) {\n                    localStorage.removeItem(storageKey);\n                }\n                return allowStale ? parsed : null;\n            }\n            return parsed;\n        }\n        catch (error) {\n            console.warn('Failed to read from localStorage:', error);\n            return null;\n        }\n    }\n    /**\n     * Strip HTML tags from text content\n     */\n    _stripHtml(html) {\n        if (!html)\n            return '';\n        // Create a temporary div element to parse HTML\n        const tempDiv = document.createElement('div');\n        tempDiv.innerHTML = html;\n        // Get text content and clean up whitespace\n        const textContent = tempDiv.textContent || tempDiv.innerText || '';\n        return textContent.trim().replace(/\\s+/g, ' ');\n    }\n    /**\n     * Ensure content has HTML markup for rich rendering.\n     * - If block-level HTML is already present, return as-is.\n     * - If WordPress rendered the content as a single <p> with no inner structure,\n     *   apply smart plain-text conversion:\n     *     • backtick spans  → <code>\n     *     • double newlines → <p> paragraph breaks\n     *     • single newlines → <br>\n     *     • inline \" - \"    → line-break + en-dash (handles \"Vars: - key: desc - key: desc\")\n     */\n    _ensureHtml(content) {\n        if (!content)\n            return '';\n        // Already contains meaningful block-level markup — leave it alone\n        if (/<(p|br|ul|ol|li|h[1-6]|blockquote)\\b/i.test(content))\n            return content;\n        // Convert backtick code spans\n        let html = content.replace(/`([^`\\n]+)`/g, '<code>$1</code>');\n        // If there are newlines use them as paragraph/line separators\n        if (html.includes('\\n')) {\n            return '<p>' + html\n                .replace(/\\n\\n+/g, '</p><p>')\n                .replace(/\\n/g, '<br>') + '</p>';\n        }\n        // No newlines: convert inline \" - \" separators to line breaks so list-style\n        // entries (\"UC Variables used: - `key`: desc - `key`: desc\") become readable\n        html = html.replace(/\\s+-\\s+/g, '<br>– ');\n        return `<p>${html}</p>`;\n    }\n    /**\n     * Decode HTML entities (e.g., &#8211; → –, &amp; → &)\n     */\n    _decodeHtmlEntities(text) {\n        if (!text)\n            return '';\n        const tempDiv = document.createElement('div');\n        tempDiv.innerHTML = text;\n        return tempDiv.textContent || tempDiv.innerText || text;\n    }\n    /**\n     * Truncate description to word limit for preset cards\n     */\n    _truncateDescription(description, wordLimit = 15) {\n        if (!description)\n            return '';\n        const words = description.split(' ');\n        if (words.length <= wordLimit) {\n            return description;\n        }\n        return words.slice(0, wordLimit).join(' ') + '...';\n    }\n    /**\n     * CORS-resilient fetch that tries multiple methods\n     */\n    async _fetchWithCorsResilience(url) {\n        // Method 1: Direct fetch (fastest if CORS is working)\n        try {\n            const response = await fetch(url, {\n                method: 'GET',\n                headers: {\n                    Accept: 'application/json',\n                    'Content-Type': 'application/json',\n                },\n                signal: AbortSignal.timeout(8000), // 8 second timeout\n            });\n            if (!response.ok) {\n                throw new Error(`HTTP ${response.status}: ${response.statusText}`);\n            }\n            return await response.json();\n        }\n        catch (error) {\n            // Direct fetch failed, trying CORS proxies silently\n            // Method 2: Try CORS proxies\n            for (const proxy of this.corsProxies) {\n                try {\n                    const proxyUrl = `${proxy}${encodeURIComponent(url)}`;\n                    const response = await fetch(proxyUrl, {\n                        method: 'GET',\n                        headers: {\n                            Accept: 'application/json',\n                        },\n                        signal: AbortSignal.timeout(10000), // 10 second timeout for proxies\n                    });\n                    if (!response.ok) {\n                        throw new Error(`Proxy HTTP ${response.status}: ${response.statusText}`);\n                    }\n                    const data = await response.json();\n                    console.log(`Successfully fetched via proxy: ${proxy}`);\n                    return data;\n                }\n                catch (proxyError) {\n                    // Proxy failed silently, try next one\n                    continue; // Try next proxy\n                }\n            }\n            // Method 3: Try JSONP-style approach (if supported by server)\n            try {\n                return await this._fetchViaJsonp(url);\n            }\n            catch (jsonpError) {\n                console.warn('JSONP fetch failed:', jsonpError);\n            }\n            // All methods failed\n            throw new Error('All CORS-resilient fetch methods failed');\n        }\n    }\n    /**\n     * JSONP-style fetch (experimental)\n     */\n    async _fetchViaJsonp(url) {\n        return new Promise((resolve, reject) => {\n            const callbackName = `ultracard_jsonp_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;\n            const script = document.createElement('script');\n            const timeoutId = setTimeout(() => {\n                cleanup();\n                reject(new Error('JSONP timeout'));\n            }, 15000);\n            const cleanup = () => {\n                if (script.parentNode) {\n                    script.parentNode.removeChild(script);\n                }\n                delete window[callbackName];\n                clearTimeout(timeoutId);\n            };\n            window[callbackName] = (data) => {\n                cleanup();\n                resolve(data);\n            };\n            script.onerror = () => {\n                cleanup();\n                reject(new Error('JSONP script error'));\n            };\n            // Try to add callback parameter (may not work with all APIs)\n            const separator = url.includes('?') ? '&' : '?';\n            script.src = `${url}${separator}callback=${callbackName}`;\n            document.head.appendChild(script);\n        });\n    }\n    /**\n     * Clean up expired cache entries proactively\n     */\n    _cleanupExpiredCache() {\n        // Skip if localStorage is disabled\n        if (this.localStorageDisabled) {\n            return;\n        }\n        try {\n            const keys = Object.keys(localStorage);\n            const presetCacheKeys = keys.filter(key => key.startsWith('wp-presets-cache-'));\n            const now = Date.now();\n            let cleanedCount = 0;\n            presetCacheKeys.forEach(key => {\n                try {\n                    const stored = localStorage.getItem(key);\n                    if (!stored)\n                        return;\n                    const parsed = JSON.parse(stored);\n                    const cacheAge = now - parsed.timestamp;\n                    // Remove expired entries (beyond regular cache duration)\n                    if (cacheAge > DirectoriesProPresetsAPI.CACHE_DURATION) {\n                        localStorage.removeItem(key);\n                        cleanedCount++;\n                    }\n                }\n                catch (_a) {\n                    // Invalid entry, remove it\n                    localStorage.removeItem(key);\n                    cleanedCount++;\n                }\n            });\n            // Also enforce cache size limit proactively\n            const remainingKeys = Object.keys(localStorage).filter(key => key.startsWith('wp-presets-cache-'));\n            if (remainingKeys.length > DirectoriesProPresetsAPI.MAX_CACHE_ENTRIES) {\n                // Sort by timestamp and remove oldest\n                const entriesWithTimestamps = remainingKeys\n                    .map(key => {\n                    try {\n                        const stored = localStorage.getItem(key);\n                        if (!stored)\n                            return null;\n                        const parsed = JSON.parse(stored);\n                        return { key, timestamp: parsed.timestamp };\n                    }\n                    catch (_a) {\n                        return { key, timestamp: 0 };\n                    }\n                })\n                    .filter(Boolean)\n                    .sort((a, b) => ((a === null || a === void 0 ? void 0 : a.timestamp) || 0) - ((b === null || b === void 0 ? void 0 : b.timestamp) || 0));\n                const toRemove = entriesWithTimestamps.slice(0, entriesWithTimestamps.length - DirectoriesProPresetsAPI.MAX_CACHE_ENTRIES);\n                toRemove.forEach(entry => {\n                    if (entry === null || entry === void 0 ? void 0 : entry.key) {\n                        localStorage.removeItem(entry.key);\n                        cleanedCount++;\n                    }\n                });\n            }\n            // Cleanup happens silently\n        }\n        catch (error) {\n            console.warn('Failed to cleanup expired cache:', error);\n        }\n    }\n    /**\n     * Aggressively clean up cache to free space (removes oldest entries)\n     */\n    _cleanupCacheAggressively() {\n        try {\n            const keys = Object.keys(localStorage);\n            const presetCacheKeys = keys.filter(key => key.startsWith('wp-presets-cache-'));\n            if (presetCacheKeys.length <= DirectoriesProPresetsAPI.MAX_CACHE_ENTRIES) {\n                return; // Already under limit\n            }\n            // Sort by timestamp (oldest first) and remove oldest entries\n            const entriesWithTimestamps = presetCacheKeys\n                .map(key => {\n                try {\n                    const stored = localStorage.getItem(key);\n                    if (!stored)\n                        return null;\n                    const parsed = JSON.parse(stored);\n                    return { key, timestamp: parsed.timestamp };\n                }\n                catch (_a) {\n                    // Invalid entry, mark for removal\n                    return { key, timestamp: 0 };\n                }\n            })\n                .filter(Boolean)\n                .sort((a, b) => ((a === null || a === void 0 ? void 0 : a.timestamp) || 0) - ((b === null || b === void 0 ? void 0 : b.timestamp) || 0));\n            // Remove oldest entries until we're under the limit\n            const entriesToRemove = entriesWithTimestamps.slice(0, entriesWithTimestamps.length - DirectoriesProPresetsAPI.MAX_CACHE_ENTRIES);\n            entriesToRemove.forEach(entry => {\n                if (entry === null || entry === void 0 ? void 0 : entry.key) {\n                    localStorage.removeItem(entry.key);\n                }\n            });\n            // Silent cleanup\n        }\n        catch (error) {\n            // Silent failure\n        }\n    }\n    /**\n     * Clear ALL preset cache entries (most aggressive cleanup)\n     */\n    _clearAllPresetCache() {\n        try {\n            const keys = Object.keys(localStorage);\n            const presetCacheKeys = keys.filter(key => key.startsWith('wp-presets-cache-'));\n            presetCacheKeys.forEach(key => {\n                try {\n                    localStorage.removeItem(key);\n                }\n                catch (_a) {\n                    // Ignore errors during cleanup\n                }\n            });\n            // Silent cleanup - no console message needed\n        }\n        catch (error) {\n            // Silent failure - no console message needed\n        }\n    }\n    /**\n     * Get fallback cache from any previous successful calls\n     */\n    _getFallbackCache() {\n        // Skip if localStorage is disabled\n        if (this.localStorageDisabled) {\n            return null;\n        }\n        try {\n            const keys = Object.keys(localStorage);\n            const presetCacheKeys = keys.filter(key => key.startsWith('wp-presets-cache-presets_'));\n            // Sort by timestamp to get the most recent\n            const sortedKeys = presetCacheKeys\n                .map(key => {\n                try {\n                    const data = localStorage.getItem(key);\n                    if (!data)\n                        return null;\n                    const parsed = JSON.parse(data);\n                    return { key, timestamp: parsed.timestamp, data: parsed.data };\n                }\n                catch (_a) {\n                    return null;\n                }\n            })\n                .filter(Boolean)\n                .sort((a, b) => ((b === null || b === void 0 ? void 0 : b.timestamp) || 0) - ((a === null || a === void 0 ? void 0 : a.timestamp) || 0));\n            if (sortedKeys.length > 0 && sortedKeys[0]) {\n                return sortedKeys[0].data;\n            }\n        }\n        catch (error) {\n            console.warn('Failed to get fallback cache:', error);\n        }\n        return null;\n    }\n}\nDirectoriesProPresetsAPI.API_BASE = 'https://ultracard.io/wp-json/wp/v2';\nDirectoriesProPresetsAPI.CACHE_KEY = 'ultra-card-directories-pro-presets-v2';\nDirectoriesProPresetsAPI.CACHE_TIMESTAMP_KEY = 'ultra-card-directories-pro-presets-v2-timestamp';\nDirectoriesProPresetsAPI.CACHE_DURATION = 5 * 60 * 1000; // 5 minutes\nDirectoriesProPresetsAPI.EXTENDED_CACHE_DURATION = 24 * 60 * 60 * 1000; // 24 hours for CORS fallback\nDirectoriesProPresetsAPI.MAX_CACHE_ENTRIES = 10; // Maximum number of cache entries to keep\nDirectoriesProPresetsAPI.MAX_ENTRY_SIZE = 500 * 1024; // 500KB max per entry (rough estimate)\n// Export singleton instance\nconst directoriesProPresetsAPI = new DirectoriesProPresetsAPI();\n\n\n//# sourceURL=webpack://ultra-card/./src/services/directories-pro-presets-api.ts?");

/***/ }),

/***/ "./src/services/uc-presets-service.ts":
/*!********************************************!*\
  !*** ./src/services/uc-presets-service.ts ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ucPresetsService: () => (/* binding */ ucPresetsService)\n/* harmony export */ });\n/* harmony import */ var _directories_pro_presets_api__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./directories-pro-presets-api */ \"./src/services/directories-pro-presets-api.ts\");\n\n/**\n * Service for managing built-in presets, user presets, and WordPress presets\n * Built-in presets are hardcoded, user presets can be imported/exported,\n * WordPress presets are fetched from ultracard.io using Directories Pro\n */\nclass UcPresetsService {\n    constructor() {\n        this._userPresets = [];\n        this._wordpressPresets = [];\n        this._wordpressLoading = false;\n        this._wordpressError = null;\n        this._listeners = new Set();\n        this._statusListeners = new Set();\n        this._loadFromStorage();\n        this._setupStorageListener();\n        // Load WordPress presets on initialization\n        this._loadWordPressPresets();\n    }\n    /**\n     * Get all presets (built-in + user presets + WordPress presets)\n     */\n    getAllPresets() {\n        return [...this._getBuiltInPresets(), ...this._userPresets, ...this._wordpressPresets];\n    }\n    /**\n     * Get presets by category\n     */\n    getPresetsByCategory(category) {\n        const allPresets = this.getAllPresets();\n        if (category === 'all') {\n            return allPresets;\n        }\n        const filtered = allPresets.filter(preset => preset.category === category);\n        return filtered;\n    }\n    /**\n     * Get a specific preset by ID\n     */\n    getPreset(id) {\n        return this.getAllPresets().find(preset => preset.id === id);\n    }\n    /**\n     * Add a user preset\n     */\n    addUserPreset(preset) {\n        const newPreset = Object.assign(Object.assign({}, preset), { id: this._generateId(), metadata: {\n                created: new Date().toISOString(),\n                updated: new Date().toISOString(),\n            } });\n        this._userPresets.push(newPreset);\n        this._saveToStorage();\n        this._notifyListeners();\n        this._broadcastChange();\n        return newPreset;\n    }\n    /**\n     * Remove a user preset\n     */\n    removeUserPreset(id) {\n        const index = this._userPresets.findIndex(p => p.id === id);\n        if (index === -1)\n            return false;\n        this._userPresets.splice(index, 1);\n        this._saveToStorage();\n        this._notifyListeners();\n        this._broadcastChange();\n        return true;\n    }\n    /**\n     * Export a preset for sharing\n     * Reverses entity mappings to restore original entity names\n     */\n    exportPreset(preset) {\n        // If preset has entity mappings, reverse them before export\n        if (preset.metadata.entityMappings && preset.metadata.entityMappings.length > 0) {\n            // Import entity mapper to reverse mappings\n            return this._reversePresetMappings(preset);\n        }\n        // No mappings, return as-is\n        return preset;\n    }\n    /**\n     * Reverse entity mappings in a preset (for export)\n     */\n    _reversePresetMappings(preset) {\n        // Import dynamically to avoid circular dependencies\n        const { entityMapper } = __webpack_require__(/*! ./uc-entity-mapper */ \"./src/services/uc-entity-mapper.ts\");\n        const reversedMappings = entityMapper.reverseMappings(preset.metadata.entityMappings);\n        // Apply reversed mappings to the layout\n        const restoredLayout = entityMapper.applyMappingToLayout(preset.layout, reversedMappings);\n        // Return preset with restored layout and cleared mappings\n        return Object.assign(Object.assign({}, preset), { layout: restoredLayout, metadata: Object.assign(Object.assign({}, preset.metadata), { entityMappings: undefined }) });\n    }\n    /**\n     * Subscribe to preset changes\n     */\n    subscribe(callback) {\n        this._listeners.add(callback);\n        return () => this._listeners.delete(callback);\n    }\n    /**\n     * Subscribe to WordPress preset loading status\n     */\n    subscribeToStatus(callback) {\n        this._statusListeners.add(callback);\n        return () => this._statusListeners.delete(callback);\n    }\n    /**\n     * Get WordPress preset loading status\n     */\n    getWordPressStatus() {\n        return {\n            loading: this._wordpressLoading,\n            error: this._wordpressError,\n        };\n    }\n    /**\n     * Refresh WordPress presets\n     */\n    async refreshWordPressPresets() {\n        // Clear the API cache first to ensure fresh data\n        _directories_pro_presets_api__WEBPACK_IMPORTED_MODULE_0__.directoriesProPresetsAPI.clearCache();\n        await this._loadWordPressPresets(true);\n    }\n    /**\n     * Get WordPress presets count\n     */\n    getWordPressPresetsCount() {\n        return this._wordpressPresets.length;\n    }\n    /**\n     * Track preset download (for WordPress presets)\n     */\n    async trackPresetDownload(presetId) {\n        // Check if it's a WordPress preset\n        if (presetId.startsWith('wp-')) {\n            const wpId = parseInt(presetId.replace('wp-', ''));\n            if (!isNaN(wpId)) {\n                await _directories_pro_presets_api__WEBPACK_IMPORTED_MODULE_0__.directoriesProPresetsAPI.trackDownload(wpId);\n            }\n        }\n    }\n    /**\n     * Built-in presets - removed stock presets, only WordPress presets now\n     */\n    _getBuiltInPresets() {\n        // All presets now come from WordPress - no built-in presets\n        return [];\n    }\n    _generateId() {\n        return `preset-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;\n    }\n    _loadFromStorage() {\n        try {\n            const stored = localStorage.getItem(UcPresetsService.STORAGE_KEY);\n            if (stored) {\n                const parsed = JSON.parse(stored);\n                if (Array.isArray(parsed)) {\n                    this._userPresets = parsed.filter(this._isValidPreset.bind(this));\n                }\n            }\n        }\n        catch (error) {\n            console.warn('Failed to load user presets from storage:', error);\n            this._userPresets = [];\n        }\n    }\n    _saveToStorage() {\n        try {\n            localStorage.setItem(UcPresetsService.STORAGE_KEY, JSON.stringify(this._userPresets));\n        }\n        catch (error) {\n            console.warn('Failed to save user presets to storage:', error);\n        }\n    }\n    _setupStorageListener() {\n        window.addEventListener('storage', event => {\n            if (event.key === UcPresetsService.STORAGE_KEY) {\n                this._loadFromStorage();\n                this._notifyListeners();\n            }\n        });\n        window.addEventListener(UcPresetsService.SYNC_EVENT, () => {\n            this._loadFromStorage();\n            this._notifyListeners();\n        });\n    }\n    _notifyListeners() {\n        const allPresets = this.getAllPresets();\n        this._listeners.forEach(callback => callback(allPresets));\n    }\n    _notifyStatusListeners() {\n        const status = this.getWordPressStatus();\n        this._statusListeners.forEach(callback => callback(status));\n    }\n    /**\n     * Load WordPress presets from Directories Pro API\n     */\n    async _loadWordPressPresets(force = false) {\n        if (this._wordpressLoading && !force)\n            return;\n        this._wordpressLoading = true;\n        this._wordpressError = null;\n        this._notifyStatusListeners();\n        try {\n            const response = await _directories_pro_presets_api__WEBPACK_IMPORTED_MODULE_0__.directoriesProPresetsAPI.fetchPresets({\n                per_page: 50, // Load up to 50 presets initially\n                sort: 'popular', // Sort by most popular first\n            });\n            this._wordpressPresets = response.presets.map(this._convertWordPressPreset.bind(this));\n            this._wordpressError = null;\n            // Notify listeners of new presets\n            this._notifyListeners();\n        }\n        catch (error) {\n            console.error('Failed to load WordPress presets from Directories Pro:', error);\n            // Provide more specific error messages\n            let errorMessage = 'Unable to load presets from ultracard.io';\n            if (error instanceof Error) {\n                if (error.message.includes('CORS')) {\n                    errorMessage = 'Network access blocked. Using cached presets if available.';\n                }\n                else if (error.message.includes('timeout')) {\n                    errorMessage = 'Connection timeout. Please check your internet connection.';\n                }\n                else if (error.message.includes('Failed to fetch')) {\n                    errorMessage = 'Network error. Using cached presets if available.';\n                }\n                else {\n                    errorMessage = `Connection error: ${error.message}`;\n                }\n            }\n            this._wordpressError = errorMessage;\n            // If we have cached presets, don't treat this as a fatal error\n            if (this._wordpressPresets.length > 0) {\n                console.warn('Using previously loaded presets due to network error');\n                this._wordpressError = `${errorMessage} (Using cached presets)`;\n            }\n        }\n        finally {\n            this._wordpressLoading = false;\n            this._notifyStatusListeners();\n        }\n    }\n    /**\n     * Decode base64 to UTF-8 string so special characters (æ, ø, å, etc.) are preserved.\n     * Plain atob() returns a binary string interpreted as Latin-1 and corrupts UTF-8.\n     */\n    _decodeBase64Utf8(base64) {\n        const binary = atob(base64);\n        const bytes = new Uint8Array(binary.length);\n        for (let i = 0; i < binary.length; i++) {\n            bytes[i] = binary.charCodeAt(i);\n        }\n        return new TextDecoder('utf-8').decode(bytes);\n    }\n    /**\n     * Convert WordPress preset to Ultra Card PresetDefinition\n     */\n    _convertWordPressPreset(wpPreset) {\n        var _a, _b, _c, _d;\n        try {\n            // Parse the shortcode as JSON layout configuration\n            let layout;\n            let cardSettings;\n            let customVariables;\n            try {\n                // Try to parse as direct JSON first\n                layout = JSON.parse(wpPreset.shortcode);\n                // Successfully parsed direct JSON (silent)\n                // Validate that the layout has rows with actual content\n                if (!layout.rows || !Array.isArray(layout.rows) || layout.rows.length === 0) {\n                    throw new Error('Layout has no rows');\n                }\n                // Check if rows have actual modules (not just empty structure)\n                const hasModules = layout.rows.some(row => row.columns && row.columns.some(col => col.modules && col.modules.length > 0));\n                if (!hasModules) {\n                    console.warn(`Preset ${wpPreset.id} has no modules, skipping...`);\n                    throw new Error('Layout has no modules');\n                }\n            }\n            catch (parseError) {\n                // Failed to parse layout from direct JSON; will attempt shortcode fallback (silent)\n                // Try to extract JSON from Ultra Card shortcode format [ultra_card]...content...[/ultra_card]\n                // Also support [ultra-card] format with hyphen\n                try {\n                    const shortcodeMatch = wpPreset.shortcode.match(/\\[ultra[_-]card\\]([\\s\\S]*?)\\[\\/ultra[_-]card\\]/);\n                    if (shortcodeMatch) {\n                        const encodedData = shortcodeMatch[1].trim();\n                        const jsonData = this._decodeBase64Utf8(encodedData);\n                        const importData = JSON.parse(jsonData);\n                        customVariables =\n                            Array.isArray(importData.customVariables)\n                                ? importData.customVariables\n                                : Array.isArray((_a = importData.data) === null || _a === void 0 ? void 0 : _a._customVariables)\n                                    ? importData.data._customVariables\n                                    : undefined;\n                        // Decoded shortcode (silent)\n                        if (importData.type === 'ultra-card-row' && importData.data) {\n                            // This is a row export, wrap it in a layout structure\n                            layout = {\n                                rows: [importData.data],\n                            };\n                            // Converted row export to layout (silent)\n                        }\n                        else if (importData.type === 'ultra-card-full' && ((_c = (_b = importData.data) === null || _b === void 0 ? void 0 : _b.layout) === null || _c === void 0 ? void 0 : _c.rows)) {\n                            // This is a full card export - layout is nested inside data.layout\n                            layout = importData.data.layout;\n                            // Extract card-level settings from full card export\n                            cardSettings = this._extractCardSettings(importData.data);\n                        }\n                        else if (importData.data && importData.data.layout && importData.data.layout.rows) {\n                            // Alternative full card structure - layout nested in data\n                            layout = importData.data.layout;\n                            // Extract card-level settings\n                            cardSettings = this._extractCardSettings(importData.data);\n                        }\n                        else if (importData.data && importData.data.rows) {\n                            layout = importData.data;\n                        }\n                        else if (importData.layout && importData.layout.rows) {\n                            // Direct layout object with nested rows\n                            layout = importData.layout;\n                        }\n                        else if (importData.rows) {\n                            layout = importData;\n                        }\n                        else {\n                            throw new Error('No valid layout found in shortcode');\n                        }\n                    }\n                    else {\n                        throw new Error('No shortcode format found');\n                    }\n                }\n                catch (shortcodeError) {\n                    // Failed to parse shortcode - throw error to trigger error preset handling\n                    console.error(`Failed to parse shortcode for preset ${wpPreset.id}:`, shortcodeError);\n                    console.error('Raw shortcode:', ((_d = wpPreset.shortcode) === null || _d === void 0 ? void 0 : _d.substring(0, 200)) + '...');\n                    throw new Error(`Failed to parse preset shortcode: ${shortcodeError instanceof Error ? shortcodeError.message : 'Unknown error'}`);\n                }\n            }\n            // Map WordPress category to Ultra Card category\n            const category = this._mapWordPressCategory(wpPreset.category);\n            // Determine if this is a standard preset from WJD Designs\n            const isStandardPreset = wpPreset.author &&\n                (wpPreset.author.toLowerCase().includes('wayne@wjddesigns.com') ||\n                    wpPreset.author.toLowerCase().includes('wjddesigns'));\n            // Create appropriate tags\n            const baseTags = wpPreset.tags || [];\n            const additionalTags = isStandardPreset ? ['standard'] : ['community'];\n            const presetDefinition = Object.assign(Object.assign(Object.assign({ id: `wp-${wpPreset.id}`, name: wpPreset.name, description: wpPreset.description || 'No description provided', category, icon: this._getPresetIcon(wpPreset.category, wpPreset.tags), author: wpPreset.author || 'Community', version: '1.0.0', tags: [...baseTags, ...additionalTags], integrations: wpPreset.integrations || [], thumbnail: wpPreset.featured_image, gallery: wpPreset.gallery || [], layout }, (cardSettings && { cardSettings })), (customVariables && customVariables.length > 0 ? { customVariables } : {})), { metadata: {\n                    created: wpPreset.created,\n                    updated: wpPreset.updated || wpPreset.created,\n                    downloads: wpPreset.downloads,\n                    rating: wpPreset.rating,\n                    rating_count: wpPreset.rating_count || wpPreset.reviews_count || 0,\n                    featured_image: wpPreset.featured_image,\n                    gallery: wpPreset.gallery || [],\n                    date: wpPreset.created,\n                } });\n            // Add WordPress-specific data for Read More functionality\n            presetDefinition.preset_url = wpPreset.preset_url;\n            presetDefinition.description_full = wpPreset.description_full;\n            presetDefinition.rating_count = wpPreset.rating_count || wpPreset.reviews_count || 0;\n            return presetDefinition;\n        }\n        catch (error) {\n            console.error(`Error converting WordPress preset ${wpPreset.id}:`, error);\n            // Return a basic error preset\n            return {\n                id: `wp-error-${wpPreset.id}`,\n                name: wpPreset.name || 'Error Preset',\n                description: 'This preset could not be loaded properly',\n                category: 'custom',\n                icon: 'mdi:alert-circle',\n                author: wpPreset.author || 'Community',\n                version: '1.0.0',\n                tags: ['error', 'community'],\n                layout: {\n                    rows: [\n                        {\n                            id: `error-row-${wpPreset.id}`,\n                            columns: [\n                                {\n                                    id: `error-col-${wpPreset.id}`,\n                                    modules: [\n                                        {\n                                            id: `error-text-${wpPreset.id}`,\n                                            type: 'text',\n                                            text: 'Preset Loading Error',\n                                            font_size: 14,\n                                            alignment: 'center',\n                                            color: 'var(--error-color)',\n                                        },\n                                    ],\n                                },\n                            ],\n                            column_layout: '1-col',\n                        },\n                    ],\n                },\n                metadata: {\n                    created: wpPreset.created || new Date().toISOString(),\n                    updated: wpPreset.updated || new Date().toISOString(),\n                },\n            };\n        }\n    }\n    /**\n     * Map WordPress category to Ultra Card category\n     */\n    _mapWordPressCategory(wpCategory) {\n        const categoryMap = {\n            // Exact matches for Ultra Card categories\n            badges: 'badges',\n            layouts: 'layouts',\n            widgets: 'widgets',\n            custom: 'badges', // Map WordPress 'custom' to 'badges' for now\n            // Alternative names that should map to Ultra Card categories\n            badge: 'badges',\n            layout: 'layouts',\n            widget: 'widgets',\n            dashboard: 'layouts',\n            dashboards: 'layouts',\n            theme: 'custom',\n            themes: 'custom',\n        };\n        const normalized = wpCategory.toLowerCase().trim();\n        const mappedCategory = categoryMap[normalized];\n        // Mapping WordPress category (silent)\n        return mappedCategory || 'badges'; // Default to badges instead of custom\n    }\n    /**\n     * Get appropriate icon for preset based on category and tags\n     */\n    _getPresetIcon(category, tags) {\n        // Check tags first for specific icons\n        if (tags.includes('person') || tags.includes('user'))\n            return 'mdi:account-circle';\n        if (tags.includes('weather'))\n            return 'mdi:weather-partly-cloudy';\n        if (tags.includes('energy'))\n            return 'mdi:lightning-bolt';\n        if (tags.includes('climate'))\n            return 'mdi:thermostat';\n        if (tags.includes('security'))\n            return 'mdi:shield-home';\n        if (tags.includes('lights') || tags.includes('lighting'))\n            return 'mdi:lightbulb';\n        if (tags.includes('vehicle') || tags.includes('car'))\n            return 'mdi:car';\n        if (tags.includes('location'))\n            return 'mdi:map-marker';\n        // Fallback to category-based icons\n        const categoryIcons = {\n            badge: 'mdi:card-account-details',\n            badges: 'mdi:card-account-details',\n            layout: 'mdi:view-column',\n            layouts: 'mdi:view-column',\n            widget: 'mdi:widgets',\n            widgets: 'mdi:widgets',\n            dashboard: 'mdi:view-dashboard',\n            dashboards: 'mdi:view-dashboard',\n        };\n        const normalized = category.toLowerCase().trim();\n        return categoryIcons[normalized] || 'mdi:card';\n    }\n    /**\n     * Extract card-level settings from a full card export\n     */\n    _extractCardSettings(cardData) {\n        if (!cardData)\n            return undefined;\n        const settings = {};\n        let hasSettings = false;\n        // Card background\n        if (cardData.card_background !== undefined) {\n            settings.card_background = cardData.card_background;\n            hasSettings = true;\n        }\n        // Card border settings\n        if (cardData.card_border_radius !== undefined) {\n            settings.card_border_radius = cardData.card_border_radius;\n            hasSettings = true;\n        }\n        if (cardData.card_border_color !== undefined) {\n            settings.card_border_color = cardData.card_border_color;\n            hasSettings = true;\n        }\n        if (cardData.card_border_width !== undefined) {\n            settings.card_border_width = cardData.card_border_width;\n            hasSettings = true;\n        }\n        // Card padding/margin\n        if (cardData.card_padding !== undefined) {\n            settings.card_padding = cardData.card_padding;\n            hasSettings = true;\n        }\n        if (cardData.card_margin !== undefined) {\n            settings.card_margin = cardData.card_margin;\n            hasSettings = true;\n        }\n        // Card overflow\n        if (cardData.card_overflow !== undefined) {\n            settings.card_overflow = cardData.card_overflow;\n            hasSettings = true;\n        }\n        // Card shadow settings\n        if (cardData.card_shadow_enabled !== undefined) {\n            settings.card_shadow_enabled = cardData.card_shadow_enabled;\n            hasSettings = true;\n        }\n        if (cardData.card_shadow_color !== undefined) {\n            settings.card_shadow_color = cardData.card_shadow_color;\n            hasSettings = true;\n        }\n        if (cardData.card_shadow_horizontal !== undefined) {\n            settings.card_shadow_horizontal = cardData.card_shadow_horizontal;\n            hasSettings = true;\n        }\n        if (cardData.card_shadow_vertical !== undefined) {\n            settings.card_shadow_vertical = cardData.card_shadow_vertical;\n            hasSettings = true;\n        }\n        if (cardData.card_shadow_blur !== undefined) {\n            settings.card_shadow_blur = cardData.card_shadow_blur;\n            hasSettings = true;\n        }\n        if (cardData.card_shadow_spread !== undefined) {\n            settings.card_shadow_spread = cardData.card_shadow_spread;\n            hasSettings = true;\n        }\n        // Background image settings\n        if (cardData.card_background_image_type !== undefined) {\n            settings.card_background_image_type = cardData.card_background_image_type;\n            hasSettings = true;\n        }\n        if (cardData.card_background_image !== undefined) {\n            settings.card_background_image = cardData.card_background_image;\n            hasSettings = true;\n        }\n        if (cardData.card_background_size !== undefined) {\n            settings.card_background_size = cardData.card_background_size;\n            hasSettings = true;\n        }\n        if (cardData.card_background_repeat !== undefined) {\n            settings.card_background_repeat = cardData.card_background_repeat;\n            hasSettings = true;\n        }\n        if (cardData.card_background_position !== undefined) {\n            settings.card_background_position = cardData.card_background_position;\n            hasSettings = true;\n        }\n        return hasSettings ? settings : undefined;\n    }\n    _broadcastChange() {\n        window.dispatchEvent(new CustomEvent(UcPresetsService.SYNC_EVENT));\n    }\n    _isValidPreset(preset) {\n        return (preset &&\n            typeof preset.id === 'string' &&\n            typeof preset.name === 'string' &&\n            typeof preset.description === 'string' &&\n            typeof preset.category === 'string' &&\n            typeof preset.icon === 'string' &&\n            preset.layout &&\n            Array.isArray(preset.layout.rows));\n    }\n}\nUcPresetsService.STORAGE_KEY = 'ultra-card-presets';\nUcPresetsService.SYNC_EVENT = 'ultra-card-presets-changed';\n// Export singleton instance\nconst ucPresetsService = new UcPresetsService();\n\n\n//# sourceURL=webpack://ultra-card/./src/services/uc-presets-service.ts?");

/***/ })

}]);